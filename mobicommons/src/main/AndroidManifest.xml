<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.applozic.mobicommons"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
    </queries>
    <application>
        <provider
            android:authorities="com.mobicomkit.sample.applozic.provider"
            android:exported="false"
            android:grantUriPermissions="true"
            android:name="com.applozic.mobicommons.file.ALFileProvider"
            tools:node="merge">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/applozic_provider_paths" />
        </provider>
    </application>

</manifest>
